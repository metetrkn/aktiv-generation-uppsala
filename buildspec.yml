version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.12
    commands:
      # 1. Unzip the project (extracts everything, including /packages/)
      - unzip aktiv-generation-uppsala.zip -d ./

      # 2. Install Poetry from the extracted .whl
      - pip install --no-index --find-links=./packages poetry-2.1.3-py3-none-any.whl

      # 3. Configure Poetry to avoid virtualenvs
      - poetry config virtualenvs.create false

      # 4. Install dependencies (offline mode)
      - export PIP_NO_INDEX=1
      - export PIP_FIND_LINKS=./packages
      - poetry install --no-dev --no-interaction --no-ansi

  pre_build:
    commands:
      - poetry run python manage.py migrate --noinput
      - poetry run python manage.py collectstatic --noinput

  build:
    commands:
      # Re-zip the project (excluding unnecessary files)
      - zip -r app.zip . -x "*.git*" "media/*" "*.pyc" "*__pycache__*" "packages/*"

  post_build:
    commands:
      - echo "Build completed."